# this file contains the customer changes for logging file management configuration
# these values will overwrite the default values present into
# /usr/local/sandvine/etc/logging_file_management.conf.default

# Overriding the maximum UDR size to 1GB
ROOT_LOGGING_TOTAL_MAX_SIZE=1
